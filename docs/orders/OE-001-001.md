# OE-001-001 — Estrutura

## Fonte normativa
`HortiVitalMix_Manual_Mestre_Tecnico_Greenfield_v6.pdf`, Módulo 001, OE-001-001.

## Entrega implementada
- Frontend React/TypeScript com página pública responsiva e estado real de indisponibilidade.
- API Express modular com rotas, serviço e repositório PostgreSQL separados.
- `requestId` centralizado e devolvido nas respostas de API.
- Tratamento centralizado para 404 JSON, JSON inválido, payload acima de 256 KiB e erro interno sem vazamento de conexão.
- Readiness 503 sem banco e 200 apenas quando o PostgreSQL responde.
- Pool PostgreSQL real com encerramento gracioso em servidor local e sinal de encerramento da função.
- Contratos TypeScript + validação de runtime com Zod.
- Migration `0001_oe_001_001_foundation.sql` para histórico de migrations.
- Vercel preparado com build Vite, função Express em `api/index.ts`, rewrite controlado e região `gru1`.
- CI com typecheck, testes e build.

## Endpoints
- `GET /api/health`
- `GET /api/ready`
- `GET /api/v1/config`
- `GET /api/v1/environment`

## Banco Neon — evidência real
Projeto `HortiVitalMix` (`little-dew-20362357`) criado em AWS South America East 1 — São Paulo, PostgreSQL 17.

Branches:
- desenvolvimento: `development` — `br-spring-lake-acphljdw`;
- homologação: `homologation` — `br-ancient-meadow-acmkote8`.

A migration 0001 foi aplicada e conferida nos dois branches. O histórico contém:
- versão: `1`;
- nome: `oe_001_001_foundation`;
- SHA-256: `7affe9ce4ce91b115d537745c9f3bf53522450b66783a2c70c6a7633183c0563`.

A tabela `app_schema_migrations` foi inspecionada nos dois ambientes, incluindo PK, UNIQUE de nome e CHECK constraints.

## Critérios de aceite cobertos
1. Sem `DATABASE_URL`, a apresentação continua disponível e `/api/ready` retorna 503.
2. Com adaptador saudável, `/api/ready` retorna 200.
3. URL de API desconhecida responde JSON 404.
4. Payload excessivo responde 413.
5. Erro interno não devolve string de conexão, segredo ou stack ao cliente.
6. `requestId` válido é correlacionado entre header e payload.
7. UI possui loading, erro/recuperação e sucesso de apresentação.
8. Layout se adapta a desktop, tablet e mobile, com foco visível e reduced motion.
9. Neon de homologação respondeu a consulta real e contém a migration 0001 aplicada.

## CI
O workflow foi disparado no GitHub e repetido. Nas duas tentativas, o job foi encerrado antes de qualquer etapa do runner; a API do GitHub reportou zero etapas/logs de job. Portanto, o estado de CI é registrado como bloqueio externo e não como aprovação. O código mantém testes automatizados prontos para execução assim que o runner estiver disponível.

## Status de validação
Banco e schema: **VALIDADOS**. Código no GitHub: **IMPLEMENTADO**. Frontend/backend: **IMPLEMENTADOS**. Vercel: **PREPARADO, AINDA NÃO VINCULADO A UM PROJETO HORTIVITALMIX**. CI: **BLOQUEADO ANTES DAS ETAPAS PELO RUNNER/GITHUB ACTIONS**.
